#include <corgi/math/Collisions.h>
#include <corgi/math/Vec3.h>
#include <ostream>
#include <vector>

using namespace corgi;

#include <corgi/test/test.h>

using namespace std;

class TestTriangle : public corgi::test::Test
{
//public:
//
//	std::vector<Vec3> v1;
//	std::vector<Vec3> v2;
//	std::vector<Vec3> v3;
//
//	int size = 9999999;
//
//	void set_up()override
//	{
//		v1.resize(size);
//		v2.resize(size);
//		v3.resize(size);
//
//		for (int i = 0; i < size; ++i)
//		{
//			v1[i].x = float(rand() % 10000 + 1);
//			v2[i].x = float(rand() % 10000 + 1);
//			v3[i].x = float(rand() % 10000 + 1);
//		}
//	}
};
//
//TEST_F(TestTriangle, triangle_normal)
//{
//	for(int i= 0; i< size; ++i)
//	{
//		math::get_triangle_normal(v1[i], v2[i], v3[i]);
//	}
//}